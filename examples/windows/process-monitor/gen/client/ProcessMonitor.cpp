// Auto-generated by ipcgen — do not edit.
#include "ProcessMonitor.h"

namespace aether::ipc
{

int ProcessMonitor::GetProcessCount(uint32_t *count, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, ProcessMonitor::kGetProcessCount, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*count))
    {
        if (count)
            std::memcpy(count, response.data(), sizeof(*count));
    }
    return _rc;
}

int ProcessMonitor::GetProcessInfo(uint32_t pid, ProcessInfo *info, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(pid));
    std::memcpy(request.data(), &pid, sizeof(pid));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, ProcessMonitor::kGetProcessInfo, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*info))
    {
        if (info)
            std::memcpy(info, response.data(), sizeof(*info));
    }
    return _rc;
}

int ProcessMonitor::GetProcessAt(uint32_t index, ProcessInfo *info, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(index));
    std::memcpy(request.data(), &index, sizeof(index));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, ProcessMonitor::kGetProcessAt, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*info))
    {
        if (info)
            std::memcpy(info, response.data(), sizeof(*info));
    }
    return _rc;
}

void ProcessMonitor::onNotification(uint32_t serviceId, uint32_t messageId,
                                    const std::vector<uint8_t> &payload)
{
    if (serviceId != kServiceId) return;

    switch (messageId)
    {
    case ProcessMonitor::kProcessStarted:
    {
        if (payload.size() < sizeof(ProcessInfo)) break;
        ProcessInfo info;
        std::memcpy(&info, payload.data(), sizeof(info));
        onProcessStarted(info);
        break;
    }
    case ProcessMonitor::kProcessExited:
    {
        if (payload.size() < sizeof(uint32_t) + sizeof(uint32_t)) break;
        uint32_t pid;
        std::memcpy(&pid, payload.data(), sizeof(pid));
        uint32_t exitCode;
        std::memcpy(&exitCode, payload.data() + sizeof(pid), sizeof(exitCode));
        onProcessExited(pid, exitCode);
        break;
    }
    default:
        break;
    }
}

} // namespace aether::ipc
