// Auto-generated by ipcgen — do not edit.
#include "ServiceStatus.h"

namespace aether::ipc
{

int ServiceStatus::GetServiceStatus(const char *serviceName, ServiceInfo *info, uint32_t timeoutMs)
{
    if (serviceName == nullptr) return IPC_ERR_INVALID_ARGUMENT;
    std::vector<uint8_t> request(65);
    char _serviceName[65] = {};
    std::strncpy(_serviceName, serviceName, 64);
    std::memcpy(request.data(), _serviceName, 65);

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, ServiceStatus::kGetServiceStatus, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*info))
    {
        if (info)
            std::memcpy(info, response.data(), sizeof(*info));
    }
    return _rc;
}

int ServiceStatus::GetServiceCount(uint32_t *count, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, ServiceStatus::kGetServiceCount, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*count))
    {
        if (count)
            std::memcpy(count, response.data(), sizeof(*count));
    }
    return _rc;
}

int ServiceStatus::GetServiceAt(uint32_t index, ServiceInfo *info, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(index));
    std::memcpy(request.data(), &index, sizeof(index));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, ServiceStatus::kGetServiceAt, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*info))
    {
        if (info)
            std::memcpy(info, response.data(), sizeof(*info));
    }
    return _rc;
}

void ServiceStatus::onNotification(uint32_t serviceId, uint32_t messageId,
                                   const std::vector<uint8_t> &payload)
{
    if (serviceId != kServiceId) return;

    switch (messageId)
    {
    case ServiceStatus::kServiceStateChanged:
    {
        if (payload.size() < sizeof(ServiceInfo) + sizeof(ServiceState)) break;
        ServiceInfo info;
        std::memcpy(&info, payload.data(), sizeof(info));
        ServiceState oldState;
        std::memcpy(&oldState, payload.data() + sizeof(info), sizeof(oldState));
        onServiceStateChanged(info, oldState);
        break;
    }
    default:
        break;
    }
}

} // namespace aether::ipc
