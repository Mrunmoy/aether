// Auto-generated by ipcgen — do not edit.
#include "CanBusEcu.h"

namespace aether::ipc
{

int CanBusEcu::ReadPid(uint8_t pid, PidReading *reading, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(pid));
    std::memcpy(request.data(), &pid, sizeof(pid));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, CanBusEcu::kReadPid, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*reading))
    {
        if (reading)
            std::memcpy(reading, response.data(), sizeof(*reading));
    }
    return _rc;
}

int CanBusEcu::GetDtcCount(uint8_t *count, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, CanBusEcu::kGetDtcCount, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*count))
    {
        if (count)
            std::memcpy(count, response.data(), sizeof(*count));
    }
    return _rc;
}

int CanBusEcu::GetDtc(uint8_t index, DtcEntry *dtc, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(index));
    std::memcpy(request.data(), &index, sizeof(index));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, CanBusEcu::kGetDtc, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*dtc))
    {
        if (dtc)
            std::memcpy(dtc, response.data(), sizeof(*dtc));
    }
    return _rc;
}

int CanBusEcu::ClearDtc(uint8_t *clearedCount, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, CanBusEcu::kClearDtc, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*clearedCount))
    {
        if (clearedCount)
            std::memcpy(clearedCount, response.data(), sizeof(*clearedCount));
    }
    return _rc;
}

int CanBusEcu::GetVehicleInfo(VehicleInfo *info, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, CanBusEcu::kGetVehicleInfo, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*info))
    {
        if (info)
            std::memcpy(info, response.data(), sizeof(*info));
    }
    return _rc;
}

void CanBusEcu::onNotification(uint32_t serviceId, uint32_t messageId,
                               const std::vector<uint8_t> &payload)
{
    if (serviceId != kServiceId) return;

    switch (messageId)
    {
    case CanBusEcu::kCheckEngine:
    {
        if (payload.size() < sizeof(DtcEntry)) break;
        DtcEntry dtc;
        std::memcpy(&dtc, payload.data(), sizeof(dtc));
        onCheckEngine(dtc);
        break;
    }
    case CanBusEcu::kDtcCleared:
    {
        if (payload.size() < sizeof(uint8_t)) break;
        uint8_t clearedCount;
        std::memcpy(&clearedCount, payload.data(), sizeof(clearedCount));
        onDtcCleared(clearedCount);
        break;
    }
    default:
        break;
    }
}

} // namespace aether::ipc
