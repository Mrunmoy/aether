// Auto-generated by ipcgen — do not edit.
#pragma once
#include "ClientBase.h"
#include "SysMonTypes.h"
#include <cstdint>
#include <cstring>
#include <vector>

namespace aether::ipc
{

class SysMon : public ClientBase
{
public:
    using ClientBase::ClientBase;

    static constexpr uint32_t kServiceId = 0xb0f3e52cu;

    enum MethodId : uint32_t
    {
        kGetCpuUsage = 1,
        kGetMemoryInfo = 2,
        kGetLoadAverage = 3,
    };

    enum NotifyId : uint32_t
    {
        kHighCpuAlert = 1,
        kLowMemoryAlert = 2,
    };

    int GetCpuUsage(CpuUsage *usage, uint32_t timeoutMs = 2000);
    int GetMemoryInfo(MemoryInfo *info, uint32_t timeoutMs = 2000);
    int GetLoadAverage(LoadAverage *load, uint32_t timeoutMs = 2000);

protected:
    virtual void onHighCpuAlert(float cpuPercent)
    {
        (void)cpuPercent;
    }
    virtual void onLowMemoryAlert(float usedPercent, uint64_t availableKb)
    {
        (void)usedPercent;
        (void)availableKb;
    }

    void onNotification(uint32_t serviceId, uint32_t messageId,
                        const std::vector<uint8_t> &payload) override;
};

} // namespace aether::ipc
