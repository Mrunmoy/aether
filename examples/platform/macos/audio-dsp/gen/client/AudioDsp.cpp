// Auto-generated by ipcgen — do not edit.
#include "AudioDsp.h"

namespace aether::ipc
{

int AudioDsp::AddEffect(uint8_t slotIndex, EffectType type, const char *name, uint8_t *success, uint32_t timeoutMs)
{
    if (name == nullptr) return IPC_ERR_INVALID_ARGUMENT;
    std::vector<uint8_t> request(sizeof(slotIndex) + sizeof(type) + 33);
    std::memcpy(request.data(), &slotIndex, sizeof(slotIndex));
    std::memcpy(request.data() + sizeof(slotIndex), &type, sizeof(type));
    char _name[33] = {};
    std::strncpy(_name, name, 32);
    std::memcpy(request.data() + sizeof(slotIndex) + sizeof(type), _name, 33);

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, AudioDsp::kAddEffect, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*success))
    {
        if (success)
            std::memcpy(success, response.data(), sizeof(*success));
    }
    return _rc;
}

int AudioDsp::RemoveEffect(uint8_t slotIndex, uint8_t *success, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(slotIndex));
    std::memcpy(request.data(), &slotIndex, sizeof(slotIndex));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, AudioDsp::kRemoveEffect, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*success))
    {
        if (success)
            std::memcpy(success, response.data(), sizeof(*success));
    }
    return _rc;
}

int AudioDsp::SetParameter(uint8_t slotIndex, uint8_t paramIndex, float value, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(slotIndex) + sizeof(paramIndex) + sizeof(value));
    std::memcpy(request.data(), &slotIndex, sizeof(slotIndex));
    std::memcpy(request.data() + sizeof(slotIndex), &paramIndex, sizeof(paramIndex));
    std::memcpy(request.data() + sizeof(slotIndex) + sizeof(paramIndex), &value, sizeof(value));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, AudioDsp::kSetParameter, request, &response, timeoutMs);
    return _rc;
}

int AudioDsp::GetEffectSlot(uint8_t slotIndex, EffectSlot *slot, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(slotIndex));
    std::memcpy(request.data(), &slotIndex, sizeof(slotIndex));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, AudioDsp::kGetEffectSlot, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*slot))
    {
        if (slot)
            std::memcpy(slot, response.data(), sizeof(*slot));
    }
    return _rc;
}

int AudioDsp::GetSignalLevel(SignalLevel *level, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, AudioDsp::kGetSignalLevel, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*level))
    {
        if (level)
            std::memcpy(level, response.data(), sizeof(*level));
    }
    return _rc;
}

int AudioDsp::GetEffectCount(uint8_t *count, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, AudioDsp::kGetEffectCount, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*count))
    {
        if (count)
            std::memcpy(count, response.data(), sizeof(*count));
    }
    return _rc;
}

void AudioDsp::onNotification(uint32_t serviceId, uint32_t messageId,
                              const std::vector<uint8_t> &payload)
{
    if (serviceId != kServiceId) return;

    switch (messageId)
    {
    case AudioDsp::kClipDetected:
    {
        if (payload.size() < sizeof(float)) break;
        float peakDb;
        std::memcpy(&peakDb, payload.data(), sizeof(peakDb));
        onClipDetected(peakDb);
        break;
    }
    case AudioDsp::kLevelUpdate:
    {
        if (payload.size() < sizeof(SignalLevel)) break;
        SignalLevel level;
        std::memcpy(&level, payload.data(), sizeof(level));
        onLevelUpdate(level);
        break;
    }
    default:
        break;
    }
}

} // namespace aether::ipc
