// Auto-generated by ipcgen — do not edit.
#include "BlePeripheral.h"

namespace aether::ipc
{

int BlePeripheral::GetServiceCount(uint8_t *count, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, BlePeripheral::kGetServiceCount, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*count))
    {
        if (count)
            std::memcpy(count, response.data(), sizeof(*count));
    }
    return _rc;
}

int BlePeripheral::GetService(uint8_t index, BleService *svc, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(index));
    std::memcpy(request.data(), &index, sizeof(index));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, BlePeripheral::kGetService, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*svc))
    {
        if (svc)
            std::memcpy(svc, response.data(), sizeof(*svc));
    }
    return _rc;
}

int BlePeripheral::ReadCharacteristic(BleUuid serviceUuid, BleUuid charUuid, BleCharacteristic *characteristic, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(serviceUuid) + sizeof(charUuid));
    std::memcpy(request.data(), &serviceUuid, sizeof(serviceUuid));
    std::memcpy(request.data() + sizeof(serviceUuid), &charUuid, sizeof(charUuid));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, BlePeripheral::kReadCharacteristic, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*characteristic))
    {
        if (characteristic)
            std::memcpy(characteristic, response.data(), sizeof(*characteristic));
    }
    return _rc;
}

int BlePeripheral::WriteCharacteristic(BleUuid serviceUuid, BleUuid charUuid, std::array<uint8_t, 20> value, uint8_t length, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(serviceUuid) + sizeof(charUuid) + sizeof(value) + sizeof(length));
    std::memcpy(request.data(), &serviceUuid, sizeof(serviceUuid));
    std::memcpy(request.data() + sizeof(serviceUuid), &charUuid, sizeof(charUuid));
    std::memcpy(request.data() + sizeof(serviceUuid) + sizeof(charUuid), &value, sizeof(value));
    std::memcpy(request.data() + sizeof(serviceUuid) + sizeof(charUuid) + sizeof(value), &length, sizeof(length));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, BlePeripheral::kWriteCharacteristic, request, &response, timeoutMs);
    return _rc;
}

int BlePeripheral::EnableNotifications(BleUuid serviceUuid, BleUuid charUuid, uint8_t enable, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(serviceUuid) + sizeof(charUuid) + sizeof(enable));
    std::memcpy(request.data(), &serviceUuid, sizeof(serviceUuid));
    std::memcpy(request.data() + sizeof(serviceUuid), &charUuid, sizeof(charUuid));
    std::memcpy(request.data() + sizeof(serviceUuid) + sizeof(charUuid), &enable, sizeof(enable));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, BlePeripheral::kEnableNotifications, request, &response, timeoutMs);
    return _rc;
}

void BlePeripheral::onNotification(uint32_t serviceId, uint32_t messageId,
                                   const std::vector<uint8_t> &payload)
{
    if (serviceId != kServiceId) return;

    switch (messageId)
    {
    case BlePeripheral::kCharacteristicChanged:
    {
        if (payload.size() < sizeof(BleUuid) + sizeof(BleUuid) + sizeof(std::array<uint8_t, 20>) + sizeof(uint8_t)) break;
        BleUuid serviceUuid;
        std::memcpy(&serviceUuid, payload.data(), sizeof(serviceUuid));
        BleUuid charUuid;
        std::memcpy(&charUuid, payload.data() + sizeof(serviceUuid), sizeof(charUuid));
        std::array<uint8_t, 20> value;
        std::memcpy(&value, payload.data() + sizeof(serviceUuid) + sizeof(charUuid), sizeof(value));
        uint8_t length;
        std::memcpy(&length, payload.data() + sizeof(serviceUuid) + sizeof(charUuid) + sizeof(value), sizeof(length));
        onCharacteristicChanged(serviceUuid, charUuid, value, length);
        break;
    }
    default:
        break;
    }
}

} // namespace aether::ipc
