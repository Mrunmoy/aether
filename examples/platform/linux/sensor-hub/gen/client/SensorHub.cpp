// Auto-generated by ipcgen — do not edit.
#include "SensorHub.h"

namespace aether::ipc
{

int SensorHub::GetAccelerometer(AccelData *accel, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, SensorHub::kGetAccelerometer, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*accel))
    {
        if (accel)
            std::memcpy(accel, response.data(), sizeof(*accel));
    }
    return _rc;
}

int SensorHub::GetGyroscope(GyroData *gyro, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, SensorHub::kGetGyroscope, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*gyro))
    {
        if (gyro)
            std::memcpy(gyro, response.data(), sizeof(*gyro));
    }
    return _rc;
}

int SensorHub::GetEnvironment(EnvironmentData *env, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, SensorHub::kGetEnvironment, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*env))
    {
        if (env)
            std::memcpy(env, response.data(), sizeof(*env));
    }
    return _rc;
}

int SensorHub::SetSampleRate(SampleRate rate, uint32_t timeoutMs)
{
    std::vector<uint8_t> request(sizeof(rate));
    std::memcpy(request.data(), &rate, sizeof(rate));

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, SensorHub::kSetSampleRate, request, &response, timeoutMs);
    return _rc;
}

int SensorHub::Calibrate(uint8_t *success, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, SensorHub::kCalibrate, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*success))
    {
        if (success)
            std::memcpy(success, response.data(), sizeof(*success));
    }
    return _rc;
}

void SensorHub::onNotification(uint32_t serviceId, uint32_t messageId,
                               const std::vector<uint8_t> &payload)
{
    if (serviceId != kServiceId) return;

    switch (messageId)
    {
    case SensorHub::kDataReady:
    {
        if (payload.size() < sizeof(AccelData) + sizeof(GyroData)) break;
        AccelData accel;
        std::memcpy(&accel, payload.data(), sizeof(accel));
        GyroData gyro;
        std::memcpy(&gyro, payload.data() + sizeof(accel), sizeof(gyro));
        onDataReady(accel, gyro);
        break;
    }
    case SensorHub::kMotionDetected:
    {
        if (payload.size() < sizeof(float)) break;
        float magnitude;
        std::memcpy(&magnitude, payload.data(), sizeof(magnitude));
        onMotionDetected(magnitude);
        break;
    }
    default:
        break;
    }
}

} // namespace aether::ipc
