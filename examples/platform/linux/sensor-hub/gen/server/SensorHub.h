// Auto-generated by ipcgen — do not edit.
#pragma once
#include "ServiceBase.h"
#include "SensorHubTypes.h"
#include <cstdint>
#include <cstring>
#include <vector>

namespace aether::ipc
{

class SensorHub : public ServiceBase
{
public:
    using ServiceBase::ServiceBase;

    static constexpr uint32_t kServiceId = 0x90ca8a82u;

    enum MethodId : uint32_t
    {
        kGetAccelerometer = 1,
        kGetGyroscope = 2,
        kGetEnvironment = 3,
        kSetSampleRate = 4,
        kCalibrate = 5,
    };

    enum NotifyId : uint32_t
    {
        kDataReady = 1,
        kMotionDetected = 2,
    };

protected:
    virtual int handleGetAccelerometer(AccelData *accel) = 0;
    virtual int handleGetGyroscope(GyroData *gyro) = 0;
    virtual int handleGetEnvironment(EnvironmentData *env) = 0;
    virtual int handleSetSampleRate(SampleRate rate) = 0;
    virtual int handleCalibrate(uint8_t *success) = 0;

    int notifyDataReady(AccelData accel, GyroData gyro);
    int notifyMotionDetected(float magnitude);

    int onRequest(uint32_t messageId, const std::vector<uint8_t> &request,
                  std::vector<uint8_t> *response) override;
};

} // namespace aether::ipc
