// Auto-generated by ipcgen — do not edit.
#include "SysMon.h"

namespace aether::ipc
{

int SysMon::onRequest(uint32_t messageId,
                      const std::vector<uint8_t> &request,
                      std::vector<uint8_t> *response)
{
    (void)request;
    (void)response;
    switch (messageId)
    {
    case SysMon::kGetCpuUsage:
    {
        CpuUsage usage{};
        int _rc = handleGetCpuUsage(&usage);
        response->resize(sizeof(usage));
        std::memcpy(response->data(), &usage, sizeof(usage));
        return _rc;
    }
    case SysMon::kGetMemoryInfo:
    {
        MemoryInfo info{};
        int _rc = handleGetMemoryInfo(&info);
        response->resize(sizeof(info));
        std::memcpy(response->data(), &info, sizeof(info));
        return _rc;
    }
    case SysMon::kGetLoadAverage:
    {
        LoadAverage load{};
        int _rc = handleGetLoadAverage(&load);
        response->resize(sizeof(load));
        std::memcpy(response->data(), &load, sizeof(load));
        return _rc;
    }
    default:
        return IPC_ERR_INVALID_METHOD;
    }
}

int SysMon::notifyHighCpuAlert(float cpuPercent)
{
    std::vector<uint8_t> payload(sizeof(cpuPercent));
    std::memcpy(payload.data(), &cpuPercent, sizeof(cpuPercent));
    return sendNotify(kServiceId, SysMon::kHighCpuAlert, payload.data(),
                      static_cast<uint32_t>(payload.size()));
}

int SysMon::notifyLowMemoryAlert(float usedPercent, uint64_t availableKb)
{
    std::vector<uint8_t> payload(sizeof(usedPercent) + sizeof(availableKb));
    std::memcpy(payload.data(), &usedPercent, sizeof(usedPercent));
    std::memcpy(payload.data() + sizeof(usedPercent), &availableKb, sizeof(availableKb));
    return sendNotify(kServiceId, SysMon::kLowMemoryAlert, payload.data(),
                      static_cast<uint32_t>(payload.size()));
}

} // namespace aether::ipc
