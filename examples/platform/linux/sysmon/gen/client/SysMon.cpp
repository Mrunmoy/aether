// Auto-generated by ipcgen — do not edit.
#include "SysMon.h"

namespace aether::ipc
{

int SysMon::GetCpuUsage(CpuUsage *usage, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, SysMon::kGetCpuUsage, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*usage))
    {
        if (usage)
            std::memcpy(usage, response.data(), sizeof(*usage));
    }
    return _rc;
}

int SysMon::GetMemoryInfo(MemoryInfo *info, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, SysMon::kGetMemoryInfo, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*info))
    {
        if (info)
            std::memcpy(info, response.data(), sizeof(*info));
    }
    return _rc;
}

int SysMon::GetLoadAverage(LoadAverage *load, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, SysMon::kGetLoadAverage, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*load))
    {
        if (load)
            std::memcpy(load, response.data(), sizeof(*load));
    }
    return _rc;
}

void SysMon::onNotification(uint32_t serviceId, uint32_t messageId,
                            const std::vector<uint8_t> &payload)
{
    if (serviceId != kServiceId) return;

    switch (messageId)
    {
    case SysMon::kHighCpuAlert:
    {
        if (payload.size() < sizeof(float)) break;
        float cpuPercent;
        std::memcpy(&cpuPercent, payload.data(), sizeof(cpuPercent));
        onHighCpuAlert(cpuPercent);
        break;
    }
    case SysMon::kLowMemoryAlert:
    {
        if (payload.size() < sizeof(float) + sizeof(uint64_t)) break;
        float usedPercent;
        std::memcpy(&usedPercent, payload.data(), sizeof(usedPercent));
        uint64_t availableKb;
        std::memcpy(&availableKb, payload.data() + sizeof(usedPercent), sizeof(availableKb));
        onLowMemoryAlert(usedPercent, availableKb);
        break;
    }
    default:
        break;
    }
}

} // namespace aether::ipc
