# Platform examples — simulated device examples are cross-platform;
# OS-level examples are guarded inside each platform's CMakeLists.txt.
add_subdirectory(linux)
add_subdirectory(macos)
add_subdirectory(windows)
