add_subdirectory(echo)
add_subdirectory(c-echo)

# sdk-usage is a standalone consumer example for an extracted SDK tarball.
# It lives in getting-started for discovery, but it is documented and validated
# separately rather than built as part of the main tree.
