// Auto-generated by ipcgen — do not edit.
#include "ExhaustAnalyzer.h"

namespace ms::ipc
{

int ExhaustAnalyzer::GetStatus(AnalyzerStatus *status, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, ExhaustAnalyzer::kGetStatus, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*status))
    {
        if (status)
            std::memcpy(status, response.data(), sizeof(*status));
    }
    return _rc;
}

int ExhaustAnalyzer::GetCurrentReadings(GasReadings *readings, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, ExhaustAnalyzer::kGetCurrentReadings, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*readings))
    {
        if (readings)
            std::memcpy(readings, response.data(), sizeof(*readings));
    }
    return _rc;
}

int ExhaustAnalyzer::StartMeasurement(bool *started, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, ExhaustAnalyzer::kStartMeasurement, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*started))
    {
        if (started)
            std::memcpy(started, response.data(), sizeof(*started));
    }
    return _rc;
}

int ExhaustAnalyzer::StopMeasurement(bool *stopped, uint32_t timeoutMs)
{
    std::vector<uint8_t> request;

    std::vector<uint8_t> response;
    int _rc = call(kServiceId, ExhaustAnalyzer::kStopMeasurement, request, &response, timeoutMs);

    if (_rc == IPC_SUCCESS && response.size() >= sizeof(*stopped))
    {
        if (stopped)
            std::memcpy(stopped, response.data(), sizeof(*stopped));
    }
    return _rc;
}

void ExhaustAnalyzer::onNotification(uint32_t serviceId, uint32_t messageId,
                                     const std::vector<uint8_t> &payload)
{
    if (serviceId != kServiceId) return;

    switch (messageId)
    {
    case ExhaustAnalyzer::kReadingsUpdated:
    {
        if (payload.size() < sizeof(GasReadings)) break;
        GasReadings readings;
        std::memcpy(&readings, payload.data(), sizeof(readings));
        onReadingsUpdated(readings);
        break;
    }
    case ExhaustAnalyzer::kStatusChanged:
    {
        if (payload.size() < sizeof(AnalyzerStatus)) break;
        AnalyzerStatus status;
        std::memcpy(&status, payload.data(), sizeof(status));
        onStatusChanged(status);
        break;
    }
    default:
        break;
    }
}

} // namespace ms::ipc
